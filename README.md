**"But we already have [Aseprite](https://www.aseprite.org/) at home!"**

**Learning project. Work in progress.**

---

_Poorsprite_ is a tiny pixel art animation viewer.

A single animation consists of a collection of frames in a directory.
A frame is an individual file named `[integer].png`.
The ordering of the integers determines the ordering of the frames in the animation.
